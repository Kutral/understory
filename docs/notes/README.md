# Notes template (each agent appends its own file docs/notes/<agent>.md)

Append, never rewrite history. Record: what was tried, what failed, measured numbers,
screenshot paths, open questions for the orchestrator.
